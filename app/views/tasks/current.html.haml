.section

  %h1
    Mission: 
    = @task.name

  .task
    .bar
      =progress_bar((100.0 * @task.clicks / @task.clicks_to_complete).round)

  = link_to('Perform mission', {:action => 'perform'}, :remote => true, :class => 'button')


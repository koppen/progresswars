.section

  %h1
    Task: 
    = @task.name

  .task
    .bar
      =progress_bar((100.0 * @task.clicks / @task.clicks_to_complete).round)

  = link_to('Do task', {:action => 'perform'}, :remote => true, :class => 'button')


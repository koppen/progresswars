!!!
%html
  %head
    %title
      = controller.action_name
    = javascript_include_tag 'http://ajax.googleapis.com/ajax/libs/jquery/1.4.1/jquery.min.js', 'http://ajax.googleapis.com/ajax/libs/jqueryui/1.7.2/jquery-ui.min.js'
    = javascript_include_tag 'rails', 'application'
    = stylesheet_link_tag 'application'
    = csrf_meta_tag
  %body
    .header
      .branding.section
        .logo
          = link_to 'Progress Wars', root_url
        .tagline
          = tagline
      .level.section
        Level
        .value= @task.level
        .experience
          .bar
            = progress_bar(@task.level_progress)
    = yield

  - javascript_tag do
    $(document).ready(function() { setInterval('animateProgressBars()', 100); })